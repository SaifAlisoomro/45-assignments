"use strict";
let members = ['Hafeez', 'sulleman', 'haseeb', 'saif', 'soomro'];
let message = 'would you like to eat apple ,';
for (let i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
